node_modules/
.next/
*.pyc
__pycache__/
.env
.env.local
*.img.zip

# Site-specific data (per-device, do not share via git)
aircraft-db.json
adsb-config.json
airfield-config.json
testdata/*.IGC
testdata/*.igc

# Local secrets / runtime state (NEVER commit — public repo)
auth.json
remote-support.json

# Backup files
*.bak
*.bak.*

# QEMU 検証環境は開発用。リポジトリでは追跡しない
qemu-test/

# aircraft-db 破損退避(データ・共有しない)
aircraft-db.json
aircraft-db.json.corrupt-*
