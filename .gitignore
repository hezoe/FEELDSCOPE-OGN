node_modules/
.next/
*.pyc
__pycache__/
.env
.env.local
*.img.zip

# Site-specific data (per-device, do not share via git)
aircraft-db.json
adsb-config.json
airfield-config.json
testdata/*.IGC
testdata/*.igc

# Local secrets / runtime state (NEVER commit — public repo)
auth.json
remote-support.json

# Backup files
*.bak
*.bak.*

# Local QEMU test images (too large for GitHub)
qemu-test/
