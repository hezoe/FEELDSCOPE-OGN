node_modules/
.next/
*.pyc
__pycache__/
.env
.env.local
*.img.zip

# Site-specific data (per-device, do not share via git)
aircraft-db.json
adsb-config.json
airfield-config.json
testdata/*.IGC
testdata/*.igc

# Local secrets / runtime state (NEVER commit — public repo)
auth.json
remote-support.json

# Backup files
*.bak
*.bak.*

# QEMU 検証環境: 手順のスクリプトだけ残し、イメージ・カーネル・鍵は追跡しない
# (イメージは巨大、カーネル/dtb は再取得可能、qemu-key は秘密鍵)
qemu-test/*
!qemu-test/boot-rpi.sh
!qemu-test/Dockerfile
