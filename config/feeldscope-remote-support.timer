[Unit]
Description=Periodically check FEELDSCOPE remote support tunnel

[Timer]
# 起動15秒後に一度(=再起動後のON復帰)、以後毎分。Persistent で取りこぼしも補正。
OnBootSec=15
OnUnitActiveSec=60
AccuracySec=15
Persistent=true

[Install]
WantedBy=timers.target
